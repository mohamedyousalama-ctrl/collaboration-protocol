name: Reconstruct and inventory CP incident corpus v1

on:
  push:
    branches:
      - research/fan-corpus-validation-2026-08-22
    paths:
      - '.github/workflows/reconstruct-cp-incident-v1.yml'
      - 'research/fan-corpus-validation/tools/reconstruct_and_inventory.py'
      - 'archive/research-lineage-2026-01/binary/CP_Incident_Database_v1.xlsx.b64.part-*'
      - 'archive/research-lineage-2026-01/binary/CP_Incident_Database_v1_RECONSTRUCTION.md'

permissions:
  contents: write

jobs:
  reconstruct:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout research branch
        uses: actions/checkout@v4
        with:
          ref: research/fan-corpus-validation-2026-08-22
          fetch-depth: 1

      - name: Reconstruct, verify, and inventory workbook
        run: python3 research/fan-corpus-validation/tools/reconstruct_and_inventory.py

      - name: Commit verified working copy and inventory
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add research/fan-corpus-validation/work/
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
          else
            git commit -m "research: reconstruct and inventory 52-record corpus v1 [skip ci]"
            git push origin HEAD:research/fan-corpus-validation-2026-08-22
          fi
